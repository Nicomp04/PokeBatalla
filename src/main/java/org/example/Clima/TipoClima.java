package org.example.Clima;

import java.util.Arrays;
import java.util.Collections;
import java.util.List;
import java.util.Random;

public enum TipoClima {
    PELIGROSO,
    NORMAL

    /*

    private static final List<TipoClima> VALUES = Collections.unmodifiableList(Arrays.asList(values()));
    private static final int size = VALUES.size();
    private static final Random id = new Random();

    public static TipoClima climaRandom()  {
        return VALUES.get(id.nextInt(size));
    }

     */
}


